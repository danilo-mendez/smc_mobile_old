<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             BackgroundColor="White"
             x:Class="Smc.Mobile.Views.Client.WelcomePage">

  <StackLayout VerticalOptions="Start" HorizontalOptions="Center">

    <!--<Image Source="https://amde.dyteks.com/Resources/logo.png" />-->
    <Label Text="Sistema de Manejo de Casos" HorizontalOptions="CenterAndExpand" Margin="10, 40, 10, 40" FontSize="{StaticResource ExtraBigSize}"/>
    <Label Text="Haga clic en comenzar para usar el sistema de autoregistro de SMC." HorizontalOptions="CenterAndExpand" Margin="10, 40, 10, 40" FontSize="{StaticResource LargeSize}"/>

    <Button Style="{StaticResource ActionButtonStyle}"
                 HorizontalOptions="Center"
                 WidthRequest="240"
                  Margin="0, 0"
                 Command="{Binding SaveAndContinueCommand}"
                 Text="Comenzar" >
    </Button>
  </StackLayout>
  
</ContentPage>